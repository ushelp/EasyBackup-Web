
btn.add    = Add
btn.reset  = Reset
btn.update = Update

conf.afterClass                    = Execute Interceptor Class when backup after; more than one, separated by a <code>;</code>, must implements <code>cn.easyproject.easybackup.backup.interceptor.BackupAfter</code> interface
conf.backupClass                   = Custome backup function implement class, must implements <code>cn.easyproject.easybackup.backup.impls.Backup</code> interface
conf.beforeClass                   = Execute Interceptor Class when backup before; more than one, separated by a <code>;</code>, must implements <code>cn.easyproject.easybackup.backup.interceptor.BackupBefore</code> interface
conf.cmdAfter                      = Execute script command when backup before or after; more than one, separated by a <code>;</code>
conf.cmdBefore                     = Execute script command when backup before or after; more than one, separated by a <code>;</code>
conf.compress                      = Whether backup compression
conf.cron                          = Backup trigger Cron-Expressions
conf.deleteBackup                  = Auto delete backup file when send email complete
conf.deleteTargetFile              = Whether delete targetFile after backup complete
conf.dir                           = Directory to store backup file, more than one separated by <code>;</code>
conf.dirClass                      = Custome directory class to backup, must implements <code>cn.easyproject.easybackup.backup.file.impl.BackupDir</code> interface
conf.file                          = Backup file name; Default is <code>'targetFileName-yyyyMMddHHmmssS[.zip|tar|tar.gz]'</code>; you can use variable: <code>${targetFileName}</code>, <code>${name}</code>, <code>${type}</code>, <code>${value}</code>, <code>${backuptime?string("yyyyMMddHHmmssS")}</code>, <code>${backupConfiguration.XXX}</code>
conf.fileClass                     = Custome file name class to backup, must implements <code>cn.easyproject.easybackup.backup.file.impl.BackupFile</code> interface
conf.name                          = Backup service name
conf.no                            = NO
conf.off                           = OFF
conf.on                            = ON
conf.receiver                      = more than one, separated by a <code>;</code>
conf.senders                       = Invoke Senders when backup completed; more than one, separated by a <code>;</code>
conf.ssl                           = Send Mail Whether use SSL
conf.targetFileClass               = Custome target file source class, must implements <code>cn.easyproject.easybackup.backup.file.impl.TargetFile</code> interface
conf.title                         = you can use variable: <code>${targetFileName}</code>, <code>${name}</code>, <code>${type}</code>, <code>${value}</code>, <code>${backuptime}</code>, <code>${backupConfiguration.XXX}</code>
conf.value                         = Backup service <code>targetFile</code> value
conf.yes                           = YES

configurations.btn.delete          = Delete
configurations.btn.detail          = Detail
configurations.btn.disable         = Disable
configurations.btn.enable          = Enable
configurations.h1                  = BackupConfigurations
configurations.nolist              = No configuration informations.
configurations.table.header        = All configurations

configurations_errors.h1           = The backup service failed to start
configurations_errors.table.header = Failed configurations list

dashboard.all          = All
dashboard.all2         = all configuration service
dashboard.enable       = Enable
dashboard.enable2      = Set enabled services
dashboard.error        = Failure
dashboard.error2       = Job failure service
dashboard.h1           = Dashboard
dashboard.h2           = Running status
dashboard.run          = Running
dashboard.run2         = running service
dashboard.shutdown     = Shutdown
dashboard.shutdown.tip = Do you want shutdown all backup service?
dashboard.startup      = Startup
dashboard.startup.tip  = Do you want startup all backup service?
dashboard.tip          = EasyBackup service is 

globalConfiguraion.h1 = Global default configuraion

header.title = EasyBackup Web manager

help.h1 = Help

msg.delete = Sure to delete the backup service?

nav.configurations = Configurations
nav.dashboard      = Dashboard
nav.global         = Golbal configuration
nav.help           = Help
nav.new            = New configuration
nav.title          = Backup Services

new.exists = Name already exists!
new.h1     = New BackupConfiguration 

status.started = Started
status.stoped  = Stoped

web.version = 1.1.0
